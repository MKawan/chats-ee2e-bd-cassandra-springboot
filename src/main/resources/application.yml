spring:
  application:
    name: demo-chats-bd-cassandra
spring.cassandra:
  keyspace-name: chat_test
  username: 
  password: 
  schema-action: CREATE_IF_NOT_EXISTS # for dev purpose
  request:
    timeout: 10s
  connection:
    connect-timeout: 10s
    init-query-timeout: 10s

datastax.astra:
  # You must download it before
  secure-connect-bundle: src/main/resources/secure-connect-cassandra-test-chat.zip
